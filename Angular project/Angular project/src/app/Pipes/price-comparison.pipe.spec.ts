import { PriceComparisonPipe } from './price-comparison.pipe';

describe('PriceComparisonPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceComparisonPipe();
    expect(pipe).toBeTruthy();
  });
});
